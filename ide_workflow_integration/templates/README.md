# AI-Ready Project Templates Index

## Overview
This `/templates` folder contains two distinct project templates cleanly optimized with built-in AI assistant parameters:

1. **React Frontend Template**: Located in `/react-app`. Combines layout automation formatting perfectly.
2. **Node Backend Template**: Located in `/node-backend`. Orchestrates API structural bounds flawlessly.

## Concrete AI Configuration
Every template explicitly specifies its AI logic internally utilizing a distinct concrete `ai-config.json` configuration file, strictly dictating GitHub Copilot access parameters identically.
